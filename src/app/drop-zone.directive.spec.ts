import { DorpZoneDirective } from './drop-zone.directive';

describe('DorpZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DorpZoneDirective();
    expect(directive).toBeTruthy();
  });
});
